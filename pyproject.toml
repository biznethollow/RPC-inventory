[tool.vercel]
entrypoint = "api.index:handler"

[project]
name = "rpc-inventory-vercel"
version = "1.0.0"
description = "XML-RPC Inventory Server menggunakan Vercel Python Runtime"
requires-python = "~=3.12.0"
dependencies = []